export type FormInputObject = { // each filter will have its own forminputobject
    category: string;
    options: string[];
}

export type ModalDataObject = {
    category: string;
    options: string[];
    //FilterInputObject: 
};